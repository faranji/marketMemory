# Trust page: sources, pipeline, checks, formulas, versions and limitations.
import streamlit as st
st.title('Data Quality & Methodology')
st.code('Raw Sources → Cleaning → Classification → Session Alignment → Labels → Embeddings → Retrieval → Confidence → Streamlit')
# TODO: actual freshness, failures, formulas, versions, leakage controls and quality thresholds.
st.warning('Historical similarity does not guarantee future similarity, and abnormal return is not proof of causality.')
