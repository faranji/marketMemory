"""Future optional X adapter; keep the core project independent from changing access/pricing."""
def collect_x_mentions(query:str,end_time:str)->list[dict]:
    # TODO: authorised API, timestamp-safe collection, dedupe, daily aggregation, spam/bot checks.
    raise NotImplementedError
